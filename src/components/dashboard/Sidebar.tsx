"use client";

import { BarChart3, CalendarDays, ChevronLeft, LayoutDashboard, LogOut, Settings2, ShieldCheck, Users, X } from "lucide-react";

const items = [{ label: "Overview", icon: LayoutDashboard }, { label: "Registrations", icon: BarChart3 }, { label: "Teams", icon: Users }, { label: "Mentors", icon: ShieldCheck }, { label: "Submissions", icon: CalendarDays }];
type Props = { activeView: string; isOpen: boolean; isCollapsed: boolean; onNavigate: (label: string) => void; onClose: () => void; onToggleCollapse: () => void };

export default function Sidebar({ activeView, isOpen, isCollapsed, onNavigate, onClose, onToggleCollapse }: Props) { return <><div className={`sidebar-backdrop ${isOpen ? "visible" : ""}`} onClick={onClose} /><aside className={`dashboard-sidebar ${isOpen ? "open" : ""} ${isCollapsed ? "collapsed" : ""}`}><div className="brand-row"><div className="brand-mark">VH<span>26</span></div><button className="icon-button close-sidebar" aria-label="Close navigation" onClick={onClose}><X size={20} /></button></div><p className="side-label">Event workspace</p><nav>{items.map(({ label, icon: Icon }) => <button key={label} className={`nav-item ${activeView === label ? "active" : ""}`} onClick={() => { onNavigate(label); onClose(); }}><Icon size={18} /><span>{label}</span>{activeView === label && <i />}</button>)}</nav><div className="sidebar-bottom"><button className="nav-item"><Settings2 size={18} /><span>Settings</span></button><div className="event-card"><div className="event-badge"><CalendarDays size={16} /></div><div><strong>VinHack 2026</strong><p>May 17–19, 2026</p></div></div><button className="nav-item muted"><LogOut size={18} /><span>Sign out</span></button></div><button className="collapse-button" aria-label={isCollapsed ? "Expand sidebar" : "Collapse sidebar"} onClick={onToggleCollapse}><ChevronLeft size={16} /></button></aside></>; }